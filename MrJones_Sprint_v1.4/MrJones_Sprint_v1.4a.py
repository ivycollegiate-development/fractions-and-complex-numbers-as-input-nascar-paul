# to import the Fraction module from the fractions class, write the following code
from fractions import Fraction
# naming the output of the user generated input as a function that we will call a
a = Fraction (input ("Please type in a fractional number here: (ex: 3/4): "))
print (a)